{
  "name": "command-bus",
  "description": "command-bus",
  "version": "0.0.10",
  "author": "cotto",
  "files": [
    "es",
    "dist",
    "README.md",
    "LICENCE"
  ],
  "main": "dist/index.js",
  "jsnext:main": "es/index.js",
  "module": "es/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/cotttpan/command-bus.git"
  },
  "bugs": {
    "url": "https://github.com/cotttpan/command-bus/issues"
  },
  "homepage": "https://github.com/cotttpan/command-bus",
  "dependencies": {
    "@cotto/utils.ts": "^0.2.11",
    "rxjs": "^6.1.0"
  },
  "devDependencies": {
    "@cotto/tslint-config": "^0.1.4",
    "@types/jest": "^22.2.3",
    "@types/node": "^10.0.4",
    "jest": "^22.4.3",
    "np": "^2.20.1",
    "npm-run-all": "^4.1.3",
    "rimraf": "^2.6.2",
    "ts-jest": "^22.4.6",
    "ts-node": "^6.0.3",
    "tsconfig-paths": "^3.3.2",
    "tslint": "^5.10.0",
    "typescript": "^2.8.3"
  },
  "keywords": [],
  "scripts": {
    "compile": "npm-run-all compile:*",
    "compile:dist": "tsc --module commonjs --outDir dist",
    "compile:es": "tsc --module es2015 --outDir es",
    "clean": "run-p clean:**",
    "clean:dist": "rimraf dist",
    "clean:es": "rimraf es",
    "lint": "tslint src/**/*.ts",
    "release:github": "git add . && git commit -am $npm_package_version --allow-empty && git tag $npm_package_version && git push && git push --tags",
    "release": "run-s clean compile test release:github",
    "test": "NODE_ENV=test jest"
  }
}
