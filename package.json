{
  "name": "command-bus",
  "description": "command-bus",
  "version": "0.4.0",
  "author": "nullabletypo",
  "files": [
    "dist",
    "README.md",
    "LICENCE"
  ],
  "main": "dist/cjs/index.js",
  "jsnext:main": "dist/es/index.js",
  "module": "dist/es/index.js",
  "types": "dist/es/index.d.ts",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/nullabletypo/command-bus.git"
  },
  "bugs": {
    "url": "https://github.com/nullabletypo/command-bus/issues"
  },
  "homepage": "https://github.com/nullabletypo/command-bus",
  "dependencies": {
    "atomic": "https://github.com/nullabletypo/atomic",
    "rxjs": "^6.5.1"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^11.13.7",
    "eslint": "^5.16.0",
    "eslint-config-nullabletypo": "https://github.com/nullabletypo/eslint-config-nullabletypo",
    "jest": "^24.7.1",
    "npm-run-all": "^4.1.5",
    "prettier": "^1.17.0",
    "rimraf": "^2.6.3",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.1.0",
    "tsconfig-paths": "^3.8.0",
    "typescript": "^3.4.5"
  },
  "keywords": [],
  "scripts": {
    "compile": "npm-run-all compile:*",
    "compile:cjs": "tsc --module commonjs --outDir dist/cjs",
    "compile:es": "tsc --module esnext --outDir dist/es",
    "clean": "run-p clean:**",
    "clean:dist": "rimraf dist",
    "lint": "eslint src/**/*.ts",
    "release:github": "git add . && git commit -am $npm_package_version --allow-empty && git tag $npm_package_version && git push && git push --tags",
    "release": "run-s clean compile test release:github",
    "test": "jest"
  },
  "jest": {
    "preset": "ts-jest",
    "verbose": true,
    "globals": {
      "ts-jest": {
        "tsConfig": "test/tsconfig.json"
      }
    }
  }
}
