{
  "name": "command-bus",
  "description": "command-bus",
  "version": "0.3.9",
  "author": "nullabletypo",
  "files": [
    "es",
    "dist",
    "README.md",
    "LICENCE"
  ],
  "main": "dist/index.js",
  "jsnext:main": "es/index.js",
  "module": "es/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/nullabletypo/command-bus.git"
  },
  "bugs": {
    "url": "https://github.com/nullabletypo/command-bus/issues"
  },
  "homepage": "https://github.com/nullabletypo/command-bus",
  "dependencies": {
    "@nullabletypo/utils-js": "https://github.com/nullabletypo/utils-js",
    "rxjs": "^6.5.1"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^11.13.7",
    "eslint": "^5.16.0",
    "eslint-config-nullabletypo": "https://github.com/nullabletypo/eslint-config-nullabletypo",
    "jest": "^24.7.1",
    "npm-run-all": "^4.1.5",
    "prettier": "^1.17.0",
    "rimraf": "^2.6.3",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.1.0",
    "tsconfig-paths": "^3.8.0",
    "typescript": "^3.4.5"
  },
  "keywords": [],
  "scripts": {
    "compile": "npm-run-all compile:*",
    "compile:dist": "tsc --p ./tsconfig.pkg.json --module commonjs --outDir dist",
    "compile:es": "tsc --p ./tsconfig.pkg.json --module es2015 --outDir es",
    "clean": "run-p clean:**",
    "clean:dist": "rimraf dist",
    "clean:es": "rimraf es",
    "lint": "tslint src/**/*.ts",
    "release:github": "git add . && git commit -am $npm_package_version --allow-empty && git tag $npm_package_version && git push && git push --tags",
    "release": "run-s clean compile test release:github",
    "test": "jest"
  },
  "jest": {
    "preset": "ts-jest",
    "verbose": true,
    "globals": {
      "ts-jest": {
        "tsConfig": "test/tsconfig.json"
      }
    }
  }
}
